const form = document.querySelector(".login-form");

form.addEventListener("submit", function (e) {
    e.preventDefault();

    window.location.href = "/html/general(tickets)/tickets/tickets.html";
});
